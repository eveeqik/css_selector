# css_selector
I created a project based on CSS selectors, fonts, and colors to understand how styling works in web design. In this project, I used different CSS selectors such as element, class, and ID selectors to target HTML elements and apply styles.
